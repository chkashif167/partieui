<header class="{{ headerClass }}">
    {{#pageHeader}}
        <div class="content">
            <div class="actions--left">
                {{#back}}
                    <a class="btn btn-bare link-back" href="{{ backLink }}"><img src="/images/icons/24px/back.svg" alt="Back Icon"></a>
                {{/back}}
                {{#account}}
                    <a class="btn btn-bare link-account" href="#"><img src="/images/icons/24px/user-placeholder.svg" alt="User Placeholder Icon"></a>
                {{/account}}
            </div>
            <h1 class="h2 t-center">{{ title }}</h1>
            <div class="actions--right">
                {{#menuButton}}
                    <a class="btn btn-bare link-more" href="#" onclick="toggleFeedMenu()"><img src="/images/icons/24px/more.svg" alt="More Icon"></a>
                {{/menuButton}}
                {{#notifications}}
                    <a class="btn btn-bare link-notifications" href="#"><img src="/images/icons/24px/notifications-on.svg" alt="Unread Notifications Icon"></a>
                {{/notifications}}
                {{#submit}}
                    <a href="#" class="btn btn-bare btn-disabled link-post" id="post"><img src="/images/icons/24px/post.svg" alt="Post Icon"></a>
                {{/submit}}
                {{#close}}
                    <a href="{{ link.pages-home }}" class="btn btn-bare link-close"><img src="/images/icons/24px/close.svg" alt="Close Icon"></a>
                {{/close}}
                {{#textLink}}
                    <a href="#" class="btn btn-bare link-text"><span class="btn-inner">{{ textLink }}</span></a>
                {{/textLink}}
            </div>
        </div>
        {{#subtitle}}
            <p class="header-subtitle t-center">{{ subtitle }}</p>
        {{/subtitle}}
        {{#search}}
            {{> components-search }}
        {{/search}}
        {{#userList}}
            {{> components-chat-user-list }}
        {{/userList}}
    {{/pageHeader}}
</header>
